{
  "auto_create_pr": true,
  "poll_interval_seconds": 15,
  "prompt": "Integrate the Phase 5 repair executor into the operational Jules cycle without changing workflows or trusted repository state. Update scripts/run_jules_cycle.py to use the repair execution API added by vertical-slice-007 instead of calling run_cycle directly. Persist a deterministic JSON result to .autodev/runtime/jules-session.json that records completion or, for a non-completed outcome, the final repair disposition, failure kind, bounded error summary, and retry/replan counters. Preserve exit code 0 for completed sessions, 20 for PAUSE_QUOTA, 21 for HUMAN_WAIT, use 22 for REPLAN, and 1 for terminal FAIL. Do not expose secrets or tracebacks. Add focused standard-library unittest coverage for the runtime result mapping/serialization by extracting pure helpers if needed. Do not add dependencies. Do not modify GOAL.md, SPEC.md, ACCEPTANCE.md, ROADMAP.md, README.md, pyproject.toml, .github/workflows, or any .autodev files. Run exactly PYTHONPATH=src python -m unittest discover -s tests -v before finishing.",
  "schema_version": 1,
  "starting_branch": "main",
  "task_id": "vertical-slice-008",
  "timeout_seconds": 1800,
  "title": "Integrate bounded repair execution into Jules runtime"
}
